<h1>Tags</h1>
<% @tags.each do |t| %>
	<ul>
		<li><%= link_to t.name, tag_path(t) %>        <%= link_to "delete" , tag_path(t), method: :delete, data: {confirm: "Do you really want to delete the tag?"} %></li>
	</ul>

<% end %>
<%= link_to "Articles", articles_path %>
<%= link_to "Home", 'welcome/index' %>
